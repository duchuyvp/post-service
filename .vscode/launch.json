{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Docker: Remote FastAPI",
            "type": "debugpy",
            "request": "attach",
            "connect": {
                "host": "0.0.0.0",
                "port": 5678
            },
            "pathMappings": [
                {
                    "localRoot": "${workspaceFolder}",
                    "remoteRoot": "/post-service"
                }
            ],
            "preLaunchTask": "Build Docker Image: post-service"
        }
    ]
}
